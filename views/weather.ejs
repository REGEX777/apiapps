<%- include('partials/header', { title: 'weather' }) %>

<div class="flex flex-col items-center h-screen items-center justify-center">
    <div class="bg-gray-800 text-white p-6 rounded-lg shadow-lg text-center w-full max-w-md">
        <div class="space-y-4">
            <div>
                <p class="text-xl mb-2 font-semibold">Temperature: <%= currentWeather.main.temp %> °C</p>
                <p class="text-xl mb-2 font-semibold">Condition: <%= currentWeather.weather[0].description %></p>
            </div>
            <div>
                <p class="text-xl font-semibold">Air Quality Index: <%= airQuality.list[0].main.aqi %></p>
            </div>
        </div>
    </div>
    <a href="/" class="mt-8 inline-block bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded transition">
        Go back
    </a>
</div>

<%- include('partials/footer') %>
