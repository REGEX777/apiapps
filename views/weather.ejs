<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Information</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.2.7/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="flex flex-col items-center justify-center min-h-screen p-6 bg-gray-100">
    <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold mb-4 text-gray-800">Weather Information</h1>
        <form action="/weather" method="get" class="mb-4">
            <input type="text" name="city" placeholder="Enter city name" class="border rounded px-3 py-2 mb-4 w-full">
            <button type="submit" class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50">
                Get Weather
            </button>
        </form>
        
        <% if (currentWeather) { %>
            <h2 class="text-2xl font-semibold text-gray-700 mb-2">Current Weather</h2>
            <p class="text-gray-600">Temperature: <%= currentWeather.main.temp %>°C</p>
            <p class="text-gray-600">Weather: <%= currentWeather.weather[0].description %></p>
            <p class="text-gray-600">Humidity: <%= currentWeather.main.humidity %>%</p>

            <h2 class="text-2xl font-semibold text-gray-700 mt-4 mb-2">7-Day Forecast</h2>
            <% forecast.list.forEach(day => { %>
                <div class="mb-2">
                    <p class="text-gray-600">Date: <%= new Date(day.dt * 1000).toLocaleDateString() %></p>
                    <p class="text-gray-600">Temp: <%= day.temp.day %>°C</p>
                    <p class="text-gray-600">Weather: <%= day.weather[0].description %></p>
                </div>
            <% }) %>

            <h2 class="text-2xl font-semibold text-gray-700 mt-4 mb-2">Air Quality</h2>
            <p class="text-gray-600">AQI: <%= airQuality.list[0].main.aqi %></p>
            <p class="text-gray-600">CO: <%= airQuality.list[0].components.co %> µg/m³</p>
            <p class="text-gray-600">NO: <%= airQuality.list[0].components.no %> µg/m³</p>
            <p class="text-gray-600">NO₂: <%= airQuality.list[0].components.no2 %> µg/m³</p>
            <p class="text-gray-600">O₃: <%= airQuality.list[0].components.o3 %> µg/m³</p>
            <p class="text-gray-600">SO₂: <%= airQuality.list[0].components.so2 %> µg/m³</p>
            <p class="text-gray-600">PM2.5: <%= airQuality.list[0].components.pm2_5 %> µg/m³</p>
            <p class="text-gray-600">PM10: <%= airQuality.list[0].components.pm10 %> µg/m³</p>
        <% } %>
        
        <% if (!currentWeather && !forecast) { %>
            <p class="text-red-500">No data available. Please enter a valid city name.</p>
        <% } %>

        <a href="/" class="mt-4 text-blue-500 underline">Back to Home</a>
    </div>
</body>
</html>
