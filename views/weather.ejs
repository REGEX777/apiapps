<% layout('layout', { title: 'Weather Forecast' }) %>
<div class="space-y-4">
    <div class="flex items-center justify-between">
        <p class="text-xl">Temperature: <%= currentWeather.main.temp %> °C</p>
        <p class="text-xl">Condition: <%= currentWeather.weather[0].description %></p>
    </div>
    <div>
        <p class="text-xl">Air Quality Index: <%= airQuality.list[0].main.aqi %></p>
    </div>
</div>
<a href="/" class="mt-8 inline-block bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded transition">Go back</a>
